'use client';

// Re-export the useAuth hook from the AuthContext for convenience
export { useAuth } from '../contexts/AuthContext';
export type { User, AuthContextType } from '../contexts/AuthContext';